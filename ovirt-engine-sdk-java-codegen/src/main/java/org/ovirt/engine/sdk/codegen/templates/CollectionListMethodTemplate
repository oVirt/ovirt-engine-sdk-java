    /**
     * Lists $decoratorName$ objects.
$docParams$
     *
     * @return List of {@link $decoratorName$ }
     *
     * @throws ClientProtocolException
     *             Signals that HTTP/S protocol error has occurred.
     * @throws ServerException
     *             Signals that an oVirt api error has occurred.
     * @throws IOException
     *             Signals that an I/O exception of some sort has occurred.
     */
    public List<$decoratorName$> list($methodExtraParamsDef$) throws ClientProtocolException,
            ServerException, IOException {

        HttpHeaderBuilder headersBuilder = new HttpHeaderBuilder();$headersToBuild$
        List<Header> headers = headersBuilder.build();

        UrlBuilder urlBuilder = new UrlBuilder(SLASH + getName());$urlParamsToBuild$
        String url = urlBuilder.build();

        return list(url, org.ovirt.engine.sdk.entities.$publicCollectionName$.class,
                $decoratorName$.class, headers);
    }
